# 2Z03 Tasks Table Implementation Plan

> For agentic workers: REQUIRED SUB-SKILL: Use superpowers-unlimited:subagent-driven-development (recommended) or superpowers-unlimited:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Rename the visible Lectures tab to Tasks and add a saved 2Z03-only chronological schedule table.

**Architecture:** Keep lectures as the stable URL value. A typed schedule module owns 51 fixed rows and a versioned completion-storage key. A client hook restores and saves a Record of task IDs to booleans; a Tasks table consumes it only for 2Z03 + lectures.

**Tech Stack:** Next.js 16, React 19, TypeScript, CSS, localStorage, Vitest, Testing Library, GitHub Pages.

---

### Task 1: Rename the navigation label

Files:
- Modify: src/lib/navigation.ts
- Modify: src/lib/navigation.test.ts
- Modify: src/components/course-header.test.tsx
- Create: docs/ai/tasks/2026-09-11-2z03-tasks-table.md
- Modify: docs/ai/tasks/ACTIVE.md

- [ ] Step 1: Write the failing tests

Add a navigation test expecting sectionLabels.lectures to equal Tasks while buildHref(2Z03, lectures) remains /?course=2Z03&section=lectures. Change the header test to query link name Tasks and retain its current href.

- [ ] Step 2: Verify RED

Run: npm test -- --run src/lib/navigation.test.ts src/components/course-header.test.tsx

Expected: fail because the visible label is Lectures.

- [ ] Step 3: Implement and checkpoint

Change only sectionLabels.lectures to Tasks. Create the active task with Status ACTIVE, objective Build and publish the 2Z03 Tasks table, and scope limited to label, fixed schedule, completion persistence, and deployment verification. Link it from ACTIVE.md.

- [ ] Step 4: Verify and commit

Run: npm test -- --run src/lib/navigation.test.ts src/components/course-header.test.tsx

Commit: git commit -m "feat: rename lectures tab to tasks"

### Task 2: Define the fixed 2Z03 schedule with TDD

Files:
- Create: src/lib/course-tasks.ts
- Create: src/lib/course-tasks.test.ts

- [ ] Step 1: Write the failing schedule tests

Test exactly 51 rows. Assert Lecture 15 is 3.5 Method of variation of parameters on Tue, Oct 20; Assignment 3 is Mon, Oct 26 · 08:00; Midterm 2 is Thu, Nov 26 · 18:30; final row is type Exam, name Final Exam, date TBD; completion key is courses-summary:2z03:tasks:v1.

- [ ] Step 2: Verify RED

Run: npm test -- --run src/lib/course-tasks.test.ts

Expected: fail because the schedule module does not exist.

- [ ] Step 3: Implement the data contract

Export CourseTaskType as Lecture | Assignment | Lab | Midterm | Exam. Export CourseTask with id, type, name, date, sortOrder. Export TASK_COMPLETION_STORAGE_KEY as courses-summary:2z03:tasks:v1.

Add lectures 1–37 in this exact date order: Sep 10/11; Sep 15/17/18; Sep 22/24/25; Sep 29, Oct 1/2; Oct 6/8/9; Oct 20/22/23; Oct 27/29/30; Nov 3/5/6; Nov 10/12/13; Nov 17/19/20; Nov 24/26/27; Dec 1/3/4; Dec 8/10. Use supplied topics in exact order: Introduction/definitions; Initial value problems; Direction fields/autonomous equations; continued; Separable equations; Linear equations; continued; Linear and nonlinear models; Linear second-order; continued; Homogeneous constant-coefficient; continued; Undetermined coefficients; continued; Variation of parameters; continued; Cauchy–Euler; Initial value problems for linear equations; continued; Boundary value problems; Laplace definition; continued; Inverse Laplace/derivatives; Translation; continued; Rational properties; continued; Dirac delta; Separable PDEs; Classical BVPs; continued; Heat equation; Laplace equation; Wave equation; continued; Catch up/review; Catch up/review last class.

Add assignments 1–6 at Sep 24 23:59, Oct 8 23:59, Oct 26 08:00, Nov 12 23:59, Nov 26 23:59, Dec 10 23:59; labs 1–5 at Oct 1, Oct 22, Nov 5, Nov 19, Dec 3 at 23:59; Midterm 1 Oct 30 18:30; Midterm 2 Nov 26 18:30; Final Exam TBD. Give date-bearing rows deterministic numerical order, Final Exam 9999, then export a sorted courseTasks array.

- [ ] Step 4: Verify and commit

Run: npm test -- --run src/lib/course-tasks.test.ts

Commit: git commit -m "feat: add 2z03 task schedule"

### Task 3: Add checklist persistence with TDD

Files:
- Create: src/hooks/use-persistent-task-completions.ts
- Create: src/hooks/use-persistent-task-completions.test.tsx

- [ ] Step 1: Write failing hook tests

Using a Harness with toggle("assignment-1"), test empty state, saving assignment-1 as true, restoration after unmount/remount, and safe fallback for corrupt JSON or unknown IDs.

- [ ] Step 2: Verify RED

Run: npm test -- --run src/hooks/use-persistent-task-completions.test.tsx

Expected: fail because the hook module does not exist.

- [ ] Step 3: Implement

Use useState<Record<string, boolean>>({}) and a hasRestored guard. Restore only object entries whose IDs are in supplied taskIds and whose values equal true; catch malformed storage. Save JSON only after restoration. Return immutable toggle(id). Keep table task IDs module-level so restore dependencies remain stable.

- [ ] Step 4: Verify and commit

Run: npm test -- --run src/hooks/use-persistent-task-completions.test.tsx && npm test -- --run

Commit: git commit -m "feat: persist 2z03 task completions"

### Task 4: Render the Tasks table with TDD

Files:
- Create: src/components/tasks-table.tsx
- Create: src/components/tasks-table.test.tsx
- Modify: src/components/course-content.tsx
- Modify: src/components/grade-calculator.test.tsx

- [ ] Step 1: Write failing UI tests

Assert four headers exactly Type, Name, Date, Checklist; 52 total rows including header; a button named Mark Assignment 1 completed changes to Completed and persists; remount restores completion. Extend CourseContent tests so 2Z03 + lectures has Tasks, while 2DA4 + lectures and 2Z03 + Notes remain blank.

- [ ] Step 2: Verify RED

Run: npm test -- --run src/components/tasks-table.test.tsx src/components/grade-calculator.test.tsx

Expected: fail because the Tasks table does not exist.

- [ ] Step 3: Implement semantic UI

Render a main Course content landmark with a Tasks section and semantic table with four columns. Each row renders one button with text Incomplete or Completed and ARIA label Mark {task.name} completed or incomplete. Use the completion hook. In CourseContent render it only for 2Z03 + lectures; retain calculator for 2Z03 + syllabus and EmptySection otherwise. Do not nest main landmarks.

- [ ] Step 4: Verify and commit

Run: npm test -- --run src/components/tasks-table.test.tsx src/components/grade-calculator.test.tsx && npm test -- --run

Commit: git commit -m "feat: add 2z03 tasks table"

### Task 5: Style, verify, and publish

Files:
- Modify: src/app/globals.css
- Modify: scope.md
- Modify: docs/ai/current-state.md
- Modify: docs/ai/source-register.md
- Modify: docs/ai/tasks/ACTIVE.md
- Modify: docs/ai/tasks/2026-09-11-2z03-tasks-table.md

- [ ] Step 1: Add responsive styles

Create a centered white tasks-table surface using existing colours, overflow-x auto, a 44rem minimum table width, burgundy header, row dividers, and outlined completion buttons that become burgundy/white when completed. Preserve the semantic table at 390 px; scroll inside the table only.

- [ ] Step 2: Verify locally

Run: npm test -- --run && NEXT_PUBLIC_BASE_PATH=/courses-summary npm run build && test -s out/index.html && git diff --check

Expected: full suite passes, static export exists, diff is clean.

- [ ] Step 3: Browser and deployment verification

Use run-local-host and verify 2Z03 Tasks label, 51 rows, Assignment 1 completion after refresh, blank 2DA4 Tasks, zero console errors, and 390 px containment. Update scope/task/current-state/source-register; mark task complete and set ACTIVE to no active tasks. Commit/push, wait for exact Pages workflow success, and verify the same behavior at https://denzeljohnson.github.io/courses-summary/?course=2Z03&section=lectures.

