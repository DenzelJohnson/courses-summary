# 2DA4 Lecture Schedule Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-unlimited:subagent-driven-development (recommended) or superpowers-unlimited:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add 37 dated 2DA4 lecture rows from September 9 through December 9, 2026, excluding the October 12–18 break.

**Architecture:** Extend the existing typed 2DA4 task data with programmatically derived lecture rows, then merge and sort them with the current assessment rows. Reuse the existing 2DA4 table, storage key, and timeline behavior without changing their interfaces.

**Tech Stack:** TypeScript, React, Vitest, Next.js static export.

---

### Task 1: Verify the 2DA4 lecture schedule contract

**Files:**
- Modify: `src/lib/2da4-tasks.test.ts`
- Modify: `src/lib/2da4-tasks.ts`

- [ ] **Step 1: Write a failing test** that expects 47 total tasks, 37 `Lecture` rows, Lecture 1 on Wed, Sep 9, Lecture 2 on Fri, Sep 11, no rows from Oct 12–18, and Lecture 37 on Wed, Dec 9.
- [ ] **Step 2: Run** `npm test -- --run src/lib/2da4-tasks.test.ts`; verify the count/schedule assertion fails because lectures are absent.
- [ ] **Step 3: Implement** a Fall 2026 Monday/Wednesday/Friday lecture definition that excludes October 12–18, maps to `Lecture 1` through `Lecture 37`, carries noon sort times and calendar dates, and is merged with assessment rows before sorting.
- [ ] **Step 4: Run** the focused test and verify it passes.

### Task 2: Verify table integration and project memory

**Files:**
- Modify: `src/components/2da4-tasks-table.test.tsx`
- Modify: `docs/ai/current-state.md`
- Modify: `docs/ai/tasks/2026-09-13-2da4-calculator-tasks.md`

- [ ] **Step 1: Write a failing table test** that expects 48 rows including the header and confirms a lecture row is not assessment-highlighted.
- [ ] **Step 2: Run** `npm test -- --run src/components/2da4-tasks-table.test.tsx`; verify the row-count assertion fails.
- [ ] **Step 3: Update** the current-state and task records with the 37-lecture schedule and new verification evidence.
- [ ] **Step 4: Run** focused task/table tests; verify they pass.

### Task 3: Verify and publish

- [ ] **Step 1: Run** `npm test -- --run`; expect all tests to pass.
- [ ] **Step 2: Run** `npm run build`; expect static export to succeed.
- [ ] **Step 3: Review** `git diff --check` and commit the schedule, tests, memory, and plan.
- [ ] **Step 4: Merge** the feature branch into `master`, push to `origin`, and verify the GitHub Pages workflow succeeds.
