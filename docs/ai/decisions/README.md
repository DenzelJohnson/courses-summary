# Decision Records

Create a dated decision record when a material architectural or product choice would otherwise be recoverable only from chat. Use `TEMPLATE.md`; never edit history to hide a superseded decision.
