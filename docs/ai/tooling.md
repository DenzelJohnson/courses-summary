# Tooling Inventory

_Verified: 2026-09-11_

| Capability | Purpose | Status | Setup check |
|---|---|---|---|
| Git | Versioned source and portable history | Required, available | `git status --short --branch` |
| Node.js and npm | Application runtime and package management | Required, available | `node --version && npm --version` |
| Next.js, React, TypeScript | Web application | Required after install | `npm run build` |
| Vitest and Testing Library | Behavior verification | Required after install | `npm test` |
| `create-cross-platform-project` | Memory layout workflow | Required for bootstrap, available in current environment | Inspect this index and required files |
| Scope, brainstorming, planning, TDD, verification skills | Safe change workflow | Required for this setup, available in current environment | Check active task evidence |
| Browser/image inspection | Visual and responsive review | Optional, available in current environment | Use only when visual verification is needed |
| Plugins | External integrations | Unavailable and unnecessary | Do not install without explicit user request |

No credentials or account identifiers are required.
