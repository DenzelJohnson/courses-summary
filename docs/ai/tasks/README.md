# Task Workflow

Every active or blocked unit of work must appear in `ACTIVE.md` and have its own file based on `TEMPLATE.md`. Update the checkpoint before handoff, compaction, provider switching, or stopping. Remove completed tasks from the active registry but retain their task files as evidence.
